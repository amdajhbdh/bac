apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployments.yaml
  - services.yaml
  - config.yaml
  - autoscaling.yaml

commonLabels:
  app.kubernetes.io/name: bac-unified
  app.kubernetes.io/part-of: education
